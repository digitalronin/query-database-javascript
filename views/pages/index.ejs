<h1>Users</h1>

<ul>
<% users.map((user) => { %>
  <li><%= user.id %> - <%= user.first_name %> <%= user.last_name %></li>
  <ul>
    <% user.Comments.map((comment) => { %>
      <li><%= comment.body %></li>
    <% }); %>
  </ul>
<% }); %>
</ul>
